// 该文件由 API文档 自动生成，请勿手动修改！
import Mock from 'mockjs';

/**
 * 班牌
 */
export default {
  // 获取班牌信息
  'POST /api/service/getCardInfo': Mock.mock({
    result: 0,
    data: true,
  }),
  // 班牌绑定
  'POST /api/service/bindCard': Mock.mock({
    result: 0,
    data: true,
  }),
  // 班牌解绑
  'POST /api/service/cancleBindCard': Mock.mock({
    result: 0,
    data: true,
  }),
};